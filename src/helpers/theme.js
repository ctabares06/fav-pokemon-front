import { createTheme } from '@material-ui/core/styles';

const theme = createTheme({
  palette: {
    primary: {
      main: '#FB1B1B'
    },
    secondary: {
      main: '#000000'
    },
    background: {
      default: '#DFDFDF'
    },
    text: {
      primary: '#000',
      secondary: '#fff'
    }
  }
})

export default theme;